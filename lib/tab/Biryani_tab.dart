import 'package:flutter/material.dart';

class biryanitab extends StatelessWidget {
  const biryanitab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Biryani Tab')),
    );
  }
}